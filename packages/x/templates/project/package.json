{
  "name": "{{ name }}",
  "version": "1.0.0",
  "scripts": {
    "blueprint:generate": "ts-node blueprint/index.ts",
    "start:api": "{{ startApiScript }}",
    "start:admin": "{{ startAdminScript }}",
    "generate:types": "{{ generateTypesScript }}"
  },
  "dependencies": {},
  "bluelibs": {
    "type": "project"
  },
  "devDependencies": {
    "@bluelibs/x": "^1.3.0",
    "ts-node": "^10.1.0",
    "typescript": "4.4.4"
  }
}
