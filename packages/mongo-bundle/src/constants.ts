import { Token } from "@bluelibs/core";

export const MONGO_URL = new Token("MONGO_BUNDLE::MONGO_URL");
export const MONGO_CONNECTION_OPTIONS = new Token(
  "MONGO_BUNDLE::MONGO_CONNECTION_OPTIONS"
);
