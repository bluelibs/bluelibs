import { Bundle } from "@bluelibs/core";

export class ValidatorBundle extends Bundle {
  async init() {
    // Currently we do nothing
    // At a later stage we may want to override the "validator" class or offer extensions
  }
}
